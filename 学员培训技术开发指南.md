# GenieSim + VLN 技术开发指南

Genie Sim 是智元发布的首个大语言模型驱动的开源仿真平台。传统仿真难以兼顾视觉逼真与物理真实，Genie Sim 开创性地将三维重建、视觉生成技术与物理引擎深度融合，实现了视觉真实感与物理精确性的统一。平台首创大语言模型驱动的场景泛化技术，让万级场景的生成只需几分钟；同步开源包含真实机器人作业场景的上万小时仿真数据集；并构建了覆盖 10 万+ 场景的多维度智能评估体系，为模型能力绘制全景画像。

本文面向课程学员，目标是帮助大家基于 Genie Sim 完成一条完整的 **视觉语言导航（VLN）开发闭环**：

```text
数据准备 → 仿真环境验证 → 数据采集 → 模型微调 → 实时推理评测 → 结果分析与优化
```

本指南只列出关键步骤和开发目标。每个阶段的详细命令、参数解释和问题排查，请参考项目根目录下的 `stage0.md`、`stage1.md`、`stage2.md`、`stage3.md`。

---

## 1. 培训目标

完成本课程后，学员应能够：

1. 完成当天课程场景文件的数据准备。
2. 在 GenieSim / Isaac Sim 中启动机器人导航仿真环境。
3. 理解 VLN 任务的数据格式和采集流程。
4. 基于自采集数据微调 StreamVLN 模型。
5. 在仿真环境中运行微调后的模型进行实时导航。
6. 根据推理结果分析失败原因，并提出数据、训练或策略层面的改进方案。

---

## 2. 项目整体流程

| 阶段 | 名称 | 核心任务 | 主要产物 |
| --- | --- | --- | --- |
| Stage 0 | 环境验证 | 启动仿真场景，用键盘控制机器人 | 可运行的 GenieSim VLN 仿真端 |
| Stage 1 | 数据采集 | 选择起点区域和目标点，生成导航轨迹数据 | `annotations.json`、RGB / depth 轨迹数据 |
| Stage 2 | 模型微调 | 使用采集数据对 StreamVLN 做 LoRA fine-tune | LoRA adapter 权重 |
| Stage 3 | 实时推理 | 加载微调模型，在仿真中执行自然语言导航指令 | 推理日志、轨迹图像、评测结果 |

推荐学习顺序：

```text
stage0.md → stage1.md → stage2.md → stage3.md
```

---

## 3. 数据准备

现场提供一个场景点云文件 `sample.ply`。学员需要先把场景相关文件上传到服务器，确保后续仿真、采集和推理都使用同一套场景资源。

### 3.1 需要上传的文件

请将本地场景数据对应上传到服务器以下路径：

| 本地文件 | 服务器路径 |
| --- | --- |
| `sample.ply` | `/root/genie_sim/scenario/sample.ply` |
| `sample.usdz` | `/root/genie_sim/source/geniesim/assets/background/my_scene/sample.usdz` |
| `sample.usda` | `/root/genie_sim/source/geniesim/assets/background/my_scene/sample.usda` |

### 3.2 准备要求

- 三个文件必须来自同一个场景版本，避免点云、贴图和材质不一致。
- `sample.usdz` 和 `sample.usda` 需要覆盖服务器上的同名文件。
- 上传完成后，再进入后续 Stage 0~3 流程。

---

## 4. 开发环境约定

课程镜像中默认项目路径为：

```bash
/root/genie_sim
```

常用环境分为两类：

| 用途 | 使用方式 |
| --- | --- |
| Isaac Sim / GenieSim 仿真端 | `/isaac-sim/python.sh` |
| StreamVLN 数据处理、训练、推理端 | `conda activate streamvln` |

注意不要混用两套 Python 环境。仿真端依赖 Isaac Sim 自带 Python，训练和模型推理依赖 `streamvln` conda 环境。

---

## 5. Stage 0：仿真环境验证

### 4.1 阶段目标

本阶段的目标不是训练模型，而是确认：

- GPU 可以被容器识别。
- Isaac Sim 可以正常启动。
- GenieSim 场景和机器人可以加载。
- 键盘控制可以通过 UDP 发送动作给机器人。

### 4.2 关键步骤

进入项目目录并检查环境：

```bash
cd /root/genie_sim
ls /isaac-sim/python.sh
nvidia-smi
echo $DISPLAY
```

启动 GenieSim VLN 仿真端：

```bash
cd /root/genie_sim
export SIM_REPO_ROOT=/root/genie_sim
export ROS_DISTRO=jazzy
export RMW_IMPLEMENTATION=rmw_fastrtps_cpp
export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:/isaac-sim/exts/isaacsim.ros2.bridge/jazzy/lib

/isaac-sim/python.sh scripts/vln_app.py \
  --config source/geniesim/config/my_scene_vln.yaml \
  --/renderer/multiGpu/enabled=False \
  --/physics/cudaDevice=0
```

另开终端启动键盘控制：

```bash
cd /root/genie_sim
python3 scripts/keyboard_control.py
```

### 4.3 学员需要理解

- `vln_app.py` 是仿真服务端，负责加载场景、机器人、碰撞检测和 VLN API。
- `keyboard_control.py` 是一个简单客户端，把 `W/S/A/D/Space` 转换为离散导航动作。
- `12346` 是键盘控制 UDP 端口，`12347` 是后续模型推理调用的 VLN API 端口。
- 如果机器人撞到障碍物，仿真端会阻止动作执行，这是后续路径规划和数据质量的重要基础。

### 4.4 阶段交付

学员应能完成：

- 成功启动 Isaac Sim 场景。
- 用键盘控制机器人前进、后退、转向和停止。
- 观察并记录机器人在场景中的可通行区域和障碍物区域。

---

## 6. Stage 1：采集 StreamVLN 训练数据

### 5.1 阶段目标

本阶段目标是从 3DGS 场景中生成可用于 StreamVLN fine-tune 的导航数据。数据采集流程包含：

1. 选择机器人随机起点区域。
2. 点击目标物体前方的停靠点。
3. 自动规划从起点到目标的轨迹。
4. 渲染第一视角 RGB / depth 图像。
5. 生成 StreamVLN 训练所需标注文件。

### 5.2 关键步骤

激活训练环境：

```bash
cd /root/genie_sim
conda activate streamvln
```

运行数据采集：

```bash
python course_tools/collect_streamvln_data.py \
  --ply_path scenario/sample.ply \
  --scene_name classroom \
  --output_dir data/course/classroom_target \
  --num_episodes 50
```

采集完成后检查数据格式：

```bash
python course_tools/check_streamvln_dataset.py \
  --data_root data/course/classroom_target
```

看到下面输出，说明数据可以进入 Stage 2：

```text
OK: dataset looks compatible with StreamVLN navigation training.
```

### 5.3 学员需要理解

采集工具主要生成两类信息：

| 信息 | 作用 |
| --- | --- |
| 轨迹图像 | 模型训练时看到的第一视角视觉输入 |
| 动作与语言标注 | 模型学习“看到画面后下一步该做什么”的监督信号 |

输出目录通常包含：

```text
data/course/classroom_target/
├── target.json
├── trajectories.json
├── annotate_episodes.json
├── annotations.json
└── render_trajectory_xxxx/rgb/*.png
```

其中最重要的是：

- `annotations.json`：Stage 2 训练直接使用的标注文件。
- `render_trajectory_xxxx/rgb/`：训练使用的第一视角 RGB 帧。
- `target.json`：Stage 3 评测目标点时使用。

### 5.4 开发思考点

学员可以从两个方向改进数据质量：

1. **路径规划质量**
   - 路径是否贴墙？
   - 是否经过狭窄区域？
   - 转弯是否自然？
   - 终点朝向是否合理？

2. **语言指令质量**
   - 指令是否准确描述目标物体？
   - 是否包含关键地标？
   - 是否和实际 RGB 轨迹一致？
   - 是否明确停止条件？

数据质量通常比单纯增加训练轮数更重要。错误或模糊的指令会直接降低 Stage 3 的导航表现。

### 5.5 阶段交付

学员应提交：

- 一份可通过检查脚本的数据集目录。
- 一个明确的目标物体和目标点。
- 若干条人工检查过的 RGB 轨迹。
- 对数据质量的简要说明。

---

## 7. Stage 2：微调 StreamVLN 模型

### 6.1 阶段目标

本阶段使用 Stage 1 采集的数据，对 StreamVLN real-world 基础权重做小样本 LoRA 微调，使模型适应当前场景和目标导航任务。

基础配置：

```text
基础模型：/root/genie_sim/StreamVLN/checkpoints/streamvln_real_world
视觉塔：/root/genie_sim/StreamVLN/checkpoints/siglip-so400m-patch14-384
训练数据：/root/genie_sim/data/course/classroom_target
输出权重：/root/gpufree-data/checkpoints/course_classroom_target_from_streamvln_real_world
```

### 6.2 关键步骤

进入 StreamVLN 目录：

```bash
cd /root/genie_sim/StreamVLN
conda activate streamvln
```

设置训练环境变量：

```bash
export PYTHONPATH=/root/genie_sim/StreamVLN:/root/genie_sim/StreamVLN/streamvln:$PYTHONPATH
export WANDB_DISABLED=true
export TOKENIZERS_PARALLELISM=false
export PYTORCH_CUDA_ALLOC_CONF=max_split_size_mb:128
```

运行 LoRA 微调：

```bash
python streamvln/streamvln_train.py \
  --model_name_or_path /root/genie_sim/StreamVLN/checkpoints/streamvln_real_world \
  --video_folder /root/genie_sim/data/course/classroom_target \
  --lora_enable True \
  --lora_r 8 \
  --lora_alpha 16 \
  --num_train_epochs 1 \
  --per_device_train_batch_size 1 \
  --output_dir /root/gpufree-data/checkpoints/course_classroom_target_from_streamvln_real_world \
  --vision_tower /root/genie_sim/StreamVLN/checkpoints/siglip-so400m-patch14-384 \
  --attn_implementation flash_attention_2 \
  --report_to none
```

上面只保留了关键参数。完整推荐训练命令请使用 `stage2.md` 中的版本。

### 6.3 学员需要理解

训练时重点观察：

| 指标 | 含义 |
| --- | --- |
| `loss` | 模型对训练样本的拟合情况 |
| `grad_norm` | 梯度是否正常更新 |
| 显存占用 | 当前超参数是否适合所用 GPU |

训练输出通常包含：

```text
adapter_config.json
adapter_model.bin 或 adapter_model.safetensors
non_lora_trainables.bin
training_args.bin
```

Stage 3 推理时需要同时加载：

```text
基础模型 + LoRA adapter + 本地视觉塔
```

### 6.4 开发思考点

学员可以围绕下面变量做小规模对比实验：

| 方向 | 可调整内容 |
| --- | --- |
| LoRA 容量 | `lora_r`、`lora_alpha`、`lora_dropout` |
| 学习强度 | `learning_rate`、`num_train_epochs` |
| 视觉上下文 | `num_history`、`num_frames` |
| 数据质量 | 数据量、起点分布、目标点选择、指令质量 |

注意：训练 loss 降低不一定代表导航成功率更高。最终判断标准应回到 Stage 3 的真实仿真推理效果。

### 6.5 阶段交付

学员应提交：

- 一个可加载的 LoRA adapter 目录。
- 训练日志中的关键 loss / grad_norm 片段。
- 本次训练使用的数据量和主要超参数。
- 对训练是否正常的初步判断。

---

## 8. Stage 3：实时推理与仿真评测

### 7.1 阶段目标

本阶段在 GenieSim 中加载 Stage 2 训练得到的 LoRA adapter，输入自然语言导航指令，观察机器人是否能到达目标物体前并正确停止。

### 7.2 关键步骤

先启动仿真端：

```bash
cd /root/genie_sim
export ROS_DISTRO=jazzy
export RMW_IMPLEMENTATION=rmw_fastrtps_cpp
export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:/isaac-sim/exts/isaacsim.ros2.bridge/jazzy/lib
export SIM_REPO_ROOT=/root/genie_sim

/isaac-sim/python.sh scripts/vln_app.py \
  --config source/geniesim/config/my_scene_vln.yaml \
  --/renderer/multiGpu/enabled=False \
  --/physics/cudaDevice=0
```

再启动模型推理端：

```bash
cd /root/genie_sim/StreamVLN
conda activate streamvln

python run_geniesim.py \
  --model_path /root/genie_sim/StreamVLN/checkpoints/streamvln_real_world \
  --adapter_path /root/gpufree-data/checkpoints/course_classroom_target_from_streamvln_real_world \
  --vision_tower_path /root/genie_sim/StreamVLN/checkpoints/siglip-so400m-patch14-384 \
  --instruction "Go to the target object and stop in front of it." \
  --target_json /root/genie_sim/data/course/classroom_target/target.json \
  --success_radius 1.0 \
  --save_images \
  --output_dir /root/genie_sim/StreamVLN/runs
```

### 7.3 学员需要理解

推理过程本质上是一个闭环：

```text
仿真端返回当前第一视角图像
        ↓
StreamVLN 根据图像和语言指令预测动作
        ↓
动作发送给 GenieSim 机器人执行
        ↓
仿真环境更新机器人位置
        ↓
继续下一步，直到 stop 或达到最大步数
```

重点评测指标包括：

| 指标 | 含义 |
| --- | --- |
| `success` | 是否在目标半径内正确 stop |
| `oracle_success` | 过程中是否曾经到过目标附近 |
| `final_distance_to_goal` | 最终位置距离目标多远 |
| `min_distance_to_goal` | 过程中距离目标最近是多少 |
| `path_length` | 实际移动路径长度 |
| `num_steps` | 执行动作步数 |
| `action_counts` | 不同动作的出现次数 |

### 7.4 结果分析方法

如果推理失败，建议按下面顺序排查：

1. **看保存图像**：模型是否真的看到了目标物体或关键地标？
2. **看动作序列**：是否一直前进、原地转圈或过早 stop？
3. **看终点距离**：是完全没接近目标，还是接近后没有 stop？
4. **回看训练数据**：训练轨迹是否覆盖类似起点和视角？
5. **回看语言指令**：测试指令是否和训练指令风格一致？

### 7.5 阶段交付

学员应提交：

- 一次完整推理的日志。
- 保存的关键 step 图像。
- `eval_result.json` 评测结果。
- 对成功或失败原因的分析。
- 下一轮改进计划。

---

## 9. 课程开发任务建议

在跑通 baseline 后，建议学员选择一个方向继续优化，而不是同时修改所有模块。

### 8.1 数据方向

可尝试：

- 增加 episode 数量。
- 扩大或收紧随机起点区域。
- 调整目标点位置和最终朝向。
- 手工改写更细粒度的导航指令。
- 删除明显不合理的轨迹样本。

适合关注的问题：

```text
数据质量是否比数据数量更重要？
不同指令风格是否影响模型导航成功率？
```

### 8.2 路径规划方向

可关注文件：

```text
course_tools/vln_collection_backend/managers/path_planner.py
```

可尝试：

- 调整障碍物膨胀半径。
- 修改 A* cost function。
- 增加路径平滑。
- 过滤过长、贴墙或转向过多的路径。

适合关注的问题：

```text
更自然的训练轨迹是否能提升推理稳定性？
```

### 8.3 训练策略方向

可尝试：

- 修改 `lora_r` / `lora_alpha`。
- 对比 1、2、3 个 epoch。
- 调整学习率。
- 改变历史帧数量和视频帧数量。

适合关注的问题：

```text
小数据场景下，模型是欠拟合还是过拟合？
训练 loss 和仿真成功率是否一致？
```

### 8.4 评测分析方向

可尝试：

- 对同一个模型测试多个指令。
- 对同一个目标测试多个起点。
- 记录成功率、最短距离、平均步数。
- 对失败案例做分类。

适合关注的问题：

```text
模型失败主要来自视觉识别、路径决策，还是停止判断？
```

---

## 10. 推荐实验记录表

每次实验建议记录下面内容：

| 项目 | 内容 |
| --- | --- |
| 实验编号 | 例如 `exp01_baseline` |
| 场景 | 使用的 `.ply` 文件 |
| 目标物体 | 目标名称和位置 |
| 数据量 | episode 数量 |
| 指令风格 | 模板指令 / 人工细化指令 |
| LoRA 参数 | `r`、`alpha`、`dropout` |
| 训练设置 | learning rate、epoch、num_history、num_frames |
| 推理结果 | success、final distance、num steps |
| 失败现象 | stop 过早、绕圈、撞障碍、找错目标等 |
| 改进计划 | 下一轮准备修改什么 |

---

## 11. 最终提交建议

课程结束时，每位学员或小组建议提交：

1. 数据集目录或数据说明。
2. LoRA adapter 权重目录。
3. 至少一次成功或有代表性的失败推理记录。
4. `eval_result.json` 或评测结果截图。
5. 一页简短实验报告，说明：
   - 任务目标是什么；
   - 做了哪些数据或训练调整；
   - 推理结果如何；
   - 失败案例说明了什么；
   - 下一步会如何改进。

---

## 12. 参考文档

- `stage0.md`：仿真启动与键盘控制详细说明。
- `stage1.md`：StreamVLN 训练数据采集详细说明。
- `stage2.md`：StreamVLN LoRA 微调详细说明。
- `stage3.md`：GenieSim 实时推理与评测详细说明。
- Genie Sim 开源项目：https://github.com/AgibotTech/genie_sim
- StreamVLN 原始开源项目：https://github.com/InternRobotics/StreamVLN.git
