# GenieSim StreamVLN 视觉语言导航课程镜像 · 使用指南

本镜像基于 [Genie Sim 3.0](https://github.com/AgibotTech/genie_sim)（智元 AgiBot 开源仿真平台）构建，预装了完整的 **VLN（视觉语言导航）教学闭环**：从仿真环境启动、导航数据采集、StreamVLN 模型 LoRA 微调，到仿真内实时推理评测。

开机即用，无需自行安装 Isaac Sim、ROS、训练框架或下载基础模型权重。适合机器人导航、具身智能、VLA/VLN 方向的教学实训与上手实验。

---

## 1. 这个镜像能做什么

镜像把一条完整的 VLN 实验流程拆成了四个阶段，前一阶段的产物正好是下一阶段的输入：

| 阶段 | 目标 | 产物 |
| --- | --- | --- |
| **Stage 0** | 启动 Isaac Sim 场景 + 键盘遥控机器人，验证仿真环境可用 | 跑通的仿真环境 |
| **Stage 1** | 在 3DGS 重建场景中采集目标导航数据 | 可训练的 StreamVLN 数据集 |
| **Stage 2** | 用采集数据对 StreamVLN 基础权重做 LoRA 微调 | LoRA adapter 权重 |
| **Stage 3** | 在 GenieSim 中加载微调模型，输入自然语言指令做实时推理与评测 | 导航成功率等评测指标 |

每个阶段的**详细操作手册**见项目根目录下的 `stage0.md` / `stage1.md` / `stage2.md` / `stage3.md`。本指南只做总览与快速开始，不重复其中的命令细节。

---

## 2. 镜像环境信息

| 项目 | 版本 / 说明 |
| --- | --- |
| 基础镜像 | `nvcr.io/nvidia/isaac-sim:5.1.0` |
| 操作系统 | Ubuntu（Isaac Sim 官方镜像基线） |
| 仿真引擎 | NVIDIA Isaac Sim **5.1.0**（支持 RTX 50 系显卡） |
| ROS | ROS 2 **Jazzy** |
| 仿真 Python | `/isaac-sim/python.sh`（运行仿真、采集、推理仿真端） |
| 训练环境 | Conda 环境 `streamvln`（PyTorch + FlashAttention2 + open3d + 3DGS 渲染依赖） |
| 加速库 | flash-attn 2.5.8、curobo 0.7.6、ik_solver 0.4.3（已预装 wheel） |
| 项目根目录 | `/root/genie_sim` |

**显卡要求**：课程流程在 **RTX 4090 24GB** 上验证通过。LoRA 微调与推理对显存较敏感，建议 **24GB 及以上**显存的 GPU。显存偏小时可参考 `stage2.md` 第 7 节下调 `num_history` / `num_frames` / `model_max_length`。

---

## 3. 预置资源与目录结构

镜像内已放好以下资源，开箱即用，无需联网下载：

```text
/root/genie_sim/
├── scenario/                         # 3DGS 重建场景（.ply）
│   ├── 1stfloor.ply
│   ├── 1stfloor_coffee.ply
│   └── home_0001.ply
├── StreamVLN/
│   ├── checkpoints/
│   │   ├── streamvln_real_world/     # StreamVLN 基础权重
│   │   └── siglip-so400m-patch14-384 # 本地 SigLIP 视觉塔（离线可用）
│   ├── run_geniesim.py               # Stage 3 实时推理脚本
│   └── streamvln/streamvln_train.py  # Stage 2 训练脚本
├── course_tools/
│   ├── collect_streamvln_data.py     # Stage 1 数据采集
│   └── check_streamvln_dataset.py    # 数据集格式检查
├── scripts/
│   ├── vln_app.py                    # Stage 0 / 3 仿真启动脚本
│   └── keyboard_control.py           # Stage 0 键盘遥控
├── 3rdparty/                         # 预置 wheel（flash-attn / curobo / ik_solver）
├── stage0.md ~ stage3.md             # 四阶段详细教程
└── README.md
```

---

## 4. 快速开始

开机后终端已经处于容器内部，提示符类似：

```bash
root@gpufree-container:~#
```

### 第 1 步：进入项目并拉取最新代码

```bash
cd /root/genie_simc
git pull
git fetch origin
git reset --hard origin/main
```

### 第 2 步：检查环境

```bash
ls /isaac-sim/python.sh   # 确认 Isaac Sim Python 存在
nvidia-smi                # 确认 GPU 可见
echo $DISPLAY             # 确认图形显示已配置（用于 Isaac Sim 弹窗）
```

> 本镜像配合 **平台自带的远程桌面 / VNC** 使用。Isaac Sim 与数据采集都会弹出 GUI 窗口，请先在平台控制台开启远程桌面，再进行后续操作。

### 第 3 步：按阶段推进

最快验证镜像可用的方式是先跑 **Stage 0**（启动仿真 + 键盘遥控）：

```bash
# 终端 1：启动 Isaac Sim 场景和机器人
cd /root/genie_sim
export SIM_REPO_ROOT=/root/genie_sim
export ROS_DISTRO=jazzy
export RMW_IMPLEMENTATION=rmw_fastrtps_cpp
export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:/isaac-sim/exts/isaacsim.ros2.bridge/jazzy/lib

/isaac-sim/python.sh scripts/vln_app.py \
  --config source/geniesim/config/my_scene_vln.yaml \
  --/renderer/multiGpu/enabled=False \
  --/physics/cudaDevice=0
```

看到 `[VLN] Scene and robot loaded successfully!` 即表示仿真环境就绪。再开一个终端运行键盘遥控，用 `W/S/A/D` 控制机器人移动，确认整套环境跑得通：

```bash
# 终端 2
cd /root/genie_sim
python3 scripts/keyboard_control.py
```

完整流程与每个参数的含义，请依次阅读 `stage0.md → stage1.md → stage2.md → stage3.md`。

---

## 5. 四阶段教程入口

| 文档 | 内容 |
| --- | --- |
| [`stage0.md`](stage0.md) | 启动 Isaac Sim 与键盘遥控，验证环境 |
| [`stage1.md`](stage1.md) | 采集 StreamVLN 导航训练数据（含路径规划、指令标注优化建议） |
| [`stage2.md`](stage2.md) | LoRA 微调 StreamVLN 基础权重（含超参调优建议） |
| [`stage3.md`](stage3.md) | 仿真内实时推理与评测（成功率、路径长度等指标） |

---

## 6. 注意事项

- **两套 Python 环境别用混**：
  - 仿真相关（Stage 0 / Stage 3 仿真端）用 **`/isaac-sim/python.sh`**；
  - 数据采集与训练（Stage 1 / 2 / 3 推理端）用 **`conda activate streamvln`**。
- **图形显示**：Isaac Sim 与数据采集预览需要 GUI。请使用平台远程桌面 / VNC；若 `echo $DISPLAY` 为空，窗口将无法弹出。数据采集若想跑无界面快速生成，可加 `--no_preview`（见 `stage1.md`）。
- **端口约定**：`12346` 为键盘控制 UDP 端口，`12347` 为 VLN / StreamVLN API TCP 端口。
- **显存**：建议 24GB 以上。同时跑训练和推理容易 OOM，先关掉多余的 GPU 进程。
- **离线可用**：基础权重与视觉塔均已本地化，`config.json` 中视觉塔应指向本地 `siglip-so400m-patch14-384`，避免运行时访问 HuggingFace（见 `stage2.md` 第 2 节）。

---

## 7. 来源与许可

- Genie Sim 项目主页：https://github.com/AgibotTech/genie_sim
- StreamVLN 原始开源项目：https://github.com/InternRobotics/StreamVLN.git
- 平台文档：https://agibot-world.com/sim-evaluation/docs/#/v3
- 论文：Genie Sim 3.0（arXiv:2601.02078）
- 许可：`source/geniesim`、`source/data_collection` 为 Mozilla Public License 2.0；其余部分见各自 LICENSE。
- StreamVLN 及第三方组件许可见 `THIRD_PARTY_LICENSES/`。
